@Library("Shared") _
pipeline {
    agent {
label "vinod"
    }

    stages {
        stage('Build') {
            steps {
                hello()
            }
        }
stage ('Clone'){
steps{
clone
}
    }
}

